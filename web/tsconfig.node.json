{
  "//": "Tests and build config run under Node, so they get Node's types; src does not.",
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "types": ["node"]
  },
  "//include": "The asset declarations come along because the tests now drive the real app, and it imports a picture.",
  "include": ["test", "wasm", "vite.config.ts", "src/assets.d.ts"]
}
